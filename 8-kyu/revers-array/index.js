// SIMPLE WAY TO REVERSE ARRAY
const fixTheMeerkat = (arr) => arr.reverse();

//VERY COOL WAY TO REARRANGE THINGS IN ARRAY
// SET UP THE ARRAY AS PARAMETERS then in the function change where the parameters are ordered
const fixTheMeerkat2 = ([tail, body, head]) => [head, body, tail]